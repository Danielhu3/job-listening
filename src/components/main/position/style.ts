import styled from "styled-components";
export const Position = styled.h2`
    grid-area: position;
    font-weight: 700;
    font-size: 1.2em;

    cursor:pointer;
    transition: all .5s ease-in-out;

    &:hover{
        color: hsl(180,29%,50%);
    }
`