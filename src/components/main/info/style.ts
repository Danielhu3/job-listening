import styled from "styled-components";
export const Info = styled.div`
    display: grid;
    grid-template-areas: 'vacancy'
                         'vacancy-title'
                         'details';

    gap: 0.5rem;
    
`